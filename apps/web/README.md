# Muchen Journey Web

Next.js App Router frontend for the single P0 exploration-camp loop. Business state and allowed commands come from the API; this package does not maintain a client-side workflow state machine.

Run the complete local system from the repository root with `docker compose up --build`. For Web-only checks, run `npm run lint`, `npm run typecheck`, and `npm run build` in this directory.
